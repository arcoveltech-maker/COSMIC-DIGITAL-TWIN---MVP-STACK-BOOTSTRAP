global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'orchestrator'
    static_configs:
      - targets: ['orchestrator:8080'] # Metrics exposed by the orchestrator

  - job_name: 'api'
    static_configs:
      - targets: ['api:8000'] # Metrics exposed by the FastAPI service
